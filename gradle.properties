org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.caching=true

modId=vantage
modName=Vantage
modVersion=0.1.0
modGroup=dev.vantage

# Architectury Loom needs this or it assumes Fabric and never creates the forge configuration
loom.platform=forge
